apiVersion: v1
kind: Service
metadata:
  name: backup-operator-webhook
  namespace: backup-system
  labels:
    app.kubernetes.io/name: backup-operator
spec:
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/name: backup-operator
