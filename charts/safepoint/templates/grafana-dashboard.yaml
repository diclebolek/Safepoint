apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/name: safepoint
data:
  safepoint.json: |
{{ .Files.Get "dashboards/safepoint.json" | indent 4 }}
